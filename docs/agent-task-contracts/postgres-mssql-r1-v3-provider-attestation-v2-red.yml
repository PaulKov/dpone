# UNBOUND until a privacy-cleared RED-assets commit exists.
schema_version: 1
task_id: POSTGRES-MSSQL-R1-V3-PROVIDER-ATTESTATION-V2-RED
title: Provider Attestation V2 RED assets
goal: Own the frozen fixtures, oracle, probe, case registry and five independently
  collecting production-facing partitions. Production modules remain read-only.
specification: docs/feature-design-postgres-mssql-r1-v3-provider-attestation-foundation-v2.md
base_commit: UNBOUND_REQUIRES_PRIVACY_CLEARANCE
integrator: dpone-maintainers
acceptance_criteria:
- Case IDs are ASCII lowercase dotted tokens, bytewise ordered and unique.
- Each partition collects independently with no collection errors or skips.
- Production-facing cases fail with ProviderAttestationMissingBehavior while
  production modules are absent.
- One valid case exists per owned foundation symbol and one rejection per partition.
public_contract_impact:
  level: none
  surfaces: []
  migration_required: false
owned_paths:
- tests/support/postgres_mssql_r1_v3_provider_attestation_v2_fixtures.py
- tests/support/postgres_mssql_r1_v3_provider_attestation_v2_oracle.py
- tests/support/postgres_mssql_r1_v3_provider_attestation_v2_red_probe.py
- tests/test_postgres_mssql_r1_v3_provider_attestation_v2_abi.py
- tests/test_postgres_mssql_r1_v3_provider_attestation_v2_query_results.py
- tests/test_postgres_mssql_r1_v3_provider_attestation_v2_stable_schema.py
- tests/test_postgres_mssql_r1_v3_provider_attestation_v2_stable_catalog.py
- tests/test_postgres_mssql_r1_v3_provider_attestation_v2_mutations.py
- docs/schemas/evidence/postgres-mssql-r1-v3-provider-attestation-v2-cases.json
integrator_owned_paths:
- docs/agent-task-contracts/postgres-mssql-r1-v3-provider-attestation-v2-red.yml
read_only_paths:
- src/dpone/contracts
- docs/feature-design-postgres-mssql-r1-v3-provider-attestation-foundation-v2.md
- docs/feature-design-postgres-mssql-r1-v3-provider-attestation-evidence-v1.md
forbidden_paths:
- pyproject.toml
- uv.lock
- CHANGELOG.md
- mkdocs.yml
- .github/workflows
shared_file_owner: dpone-maintainers
dependencies: []
required_checks:
  focused:
  - uv run pytest tests/test_postgres_mssql_r1_v3_provider_attestation_v2_abi.py tests/test_postgres_mssql_r1_v3_provider_attestation_v2_query_results.py tests/test_postgres_mssql_r1_v3_provider_attestation_v2_stable_schema.py tests/test_postgres_mssql_r1_v3_provider_attestation_v2_stable_catalog.py tests/test_postgres_mssql_r1_v3_provider_attestation_v2_mutations.py -q
  broad:
  - uv run python tools/agent_policy/select_checks.py --base-ref origin/master
  live: []
required_outputs:
- implementation
- tests
- documentation_impact
- completion_report
completion_statuses:
  allowed:
  - PASS
  - FAIL
  - SKIP
  - N/A
  - UNVERIFIED
stop_conditions:
- Required edit falls outside owned_paths.
- Approved specification is missing or contradicted.
- A live check requires unapproved credentials or environment.
