# v0.13.0 Airflow Evidence Release Cutover Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Release `v0.13.0` with the Airflow pod launch evidence gate and final Airflow evidence bundle collector.

**Architecture:** Merge the stacked Airflow train in order: first fold `codex/airflow-evidence-bundle` into `codex/airflow-pod-launch-evidence`, then update release metadata on the pod launch evidence branch, then merge the train into `master`. Create the annotated `v0.13.0` tag only after GitHub Actions is green on the exact `master` commit.

**Tech Stack:** GitHub PRs, GitHub Actions, PyPI Trusted Publishing, uv build, Twine, MkDocs, pytest, ruff, mypy.

---

### Task 1: Integrate the stacked PR train

- [ ] Merge PR #92 into `codex/airflow-pod-launch-evidence`.
- [ ] Confirm PR #91 contains pod-watch, evidence-bundle, docs, schemas, and tests.
- [ ] Wait for PR #91 checks after the stacked merge.

### Task 2: Prepare release metadata

- [ ] Bump `pyproject.toml` and `uv.lock` to `0.13.0`.
- [ ] Update `README.md`, `CHANGELOG.md`, `tests/test_oss_release_readiness.py`, and `tests/test_snapshot_version.py`.
- [ ] Keep public docs examples aligned with `0.13.0`.

### Task 3: Verify locally

- [ ] Run `uv run ruff check .`.
- [ ] Run `uv run ruff format --check .`.
- [ ] Run `uv run mypy --config-file mypy.ini`.
- [ ] Run focused GitOps/Airflow, docs, schema, and version tests.
- [ ] Run docs import/layer/module-size/metrics gates.
- [ ] Run `uv run mkdocs build --strict`.
- [ ] Run `uv run pytest -m "not integration_live" --cov=src/dpone --cov-report=xml`.
- [ ] Run `rm -rf dist && uv build && uv tool run twine check dist/*`.

### Task 4: Publish

- [ ] Push release metadata to PR #91.
- [ ] Wait for GitHub Actions on PR #91.
- [ ] Merge PR #91 to `master`.
- [ ] Wait for GitHub Actions on `master`.
- [ ] Create and push annotated tag `v0.13.0`.
- [ ] Wait for the Release workflow and runtime image workflow.
- [ ] Verify GitHub Release, PyPI resolver visibility, and public install smoke.
