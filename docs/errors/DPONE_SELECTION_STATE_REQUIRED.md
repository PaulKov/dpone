# DPONE_SELECTION_STATE_REQUIRED

A `state:*` expression needs a valid prior `dpone.selection-state.v1` baseline.
Pass `--state <selection-state.json>` or first create a local preview that
writes `.dpone-cache/current/selection-state.json`.
