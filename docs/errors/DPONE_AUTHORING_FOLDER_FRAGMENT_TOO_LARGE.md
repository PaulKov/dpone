# DPONE_AUTHORING_FOLDER_FRAGMENT_TOO_LARGE

One folder fragment exceeds 1 MiB. Keep only declarative process configuration
in the fragment and move fixtures or generated data elsewhere. Large query text
should use the existing pinned `sql_file` contract.
