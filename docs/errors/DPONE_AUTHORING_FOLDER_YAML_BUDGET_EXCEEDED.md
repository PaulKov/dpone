# DPONE_AUTHORING_FOLDER_YAML_BUDGET_EXCEEDED

The fragment exceeded the token, nesting, or node budget. Simplify the YAML and
keep reusable runtime defaults in their existing canonical configuration layer.
The safety budget cannot be bypassed with a CLI flag.
