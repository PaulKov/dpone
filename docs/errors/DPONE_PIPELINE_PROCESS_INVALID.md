# DPONE_PIPELINE_PROCESS_INVALID

One short-authoring process is not an object or lacks `name` and a complete
`source.table` schema/name. Fix the indexed process reported by `dpone check
<pipeline> --format json`.
