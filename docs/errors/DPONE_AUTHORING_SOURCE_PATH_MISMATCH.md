# DPONE_AUTHORING_SOURCE_PATH_MISMATCH

`authoring.source` does not name the file dpone actually loaded. Update the
declared project-relative path or move the file so they match exactly, then
rerun static check.
