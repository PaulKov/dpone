# DPONE_AUTHORING_FOLDER_TOTAL_SIZE_EXCEEDED

The declared fragment set exceeds 8 MiB. Split unrelated pipelines or remove
non-authoring payloads from the fragments. Credentials, fixtures, generated
data, and large SQL bodies do not belong inline in folder fragments.
