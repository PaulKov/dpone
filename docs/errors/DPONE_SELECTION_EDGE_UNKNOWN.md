# DPONE_SELECTION_EDGE_UNKNOWN

A declared dependency names a workload that is absent from the current project
catalog. Add the missing workload or correct the dependency id. Unknown edges
are never ignored because that could silently change execution order.
