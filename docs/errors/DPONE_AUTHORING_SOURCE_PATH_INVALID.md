# DPONE_AUTHORING_SOURCE_PATH_INVALID

The primary authoring path is absolute, traverses outside the project, or the
loaded file itself is outside the configured root. Use a project-relative POSIX
path such as `pipelines/orders_daily/pipeline.yaml`.
