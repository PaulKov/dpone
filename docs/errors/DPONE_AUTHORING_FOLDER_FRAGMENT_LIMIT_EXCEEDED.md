# DPONE_AUTHORING_FOLDER_FRAGMENT_LIMIT_EXCEEDED

The root lists more than 100 fragments. Group related processes so the explicit
fragment list stays within the bounded build-plane limit. dpone does not enable
recursive discovery or raise this limit per pipeline.
