# DPONE_AUTHORING_FOLDER_FRAGMENT_DUPLICATE

The same fragment path appears more than once in `fragments`. Remove the
duplicate root entry; do not copy or rename generated artifacts. Fragment order
is preserved, but every path must be unique.
