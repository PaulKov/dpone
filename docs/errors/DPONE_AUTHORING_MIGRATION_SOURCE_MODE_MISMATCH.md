# DPONE_AUTHORING_MIGRATION_SOURCE_MODE_MISMATCH

The mode supplied through `--from` differs from the mode detected by the
canonical compiler. Inspect `authoring.mode`, correct the assertion, and rerun
`--plan`. This is a configuration error; no source was changed.

See [authoring-mode migration](../airflow-authoring-migration.md).
