# DPONE_AUTHORING_MODE_KIND_MISMATCH

The source kind and `authoring.mode` disagree. Use `dpone.flow.v1` with `flow`
or `folder`, and `dpone.batch.v1` with `classic`. Do not keep two editable
representations of one pipeline.
