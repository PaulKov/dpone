# DPONE_AUTHORING_FOLDER_YAML_ALIAS_FORBIDDEN

YAML anchors or aliases were found in a fragment. Expand the values explicitly.
Folder inputs reject aliases to keep parsing bounded, reviewable, and
deterministic.
