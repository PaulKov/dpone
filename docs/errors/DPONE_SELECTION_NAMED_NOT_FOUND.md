# DPONE_SELECTION_NAMED_NOT_FOUND

`selector:<name>` refers to a named selector that is not defined in the
configured `selectors.yaml`. Correct the name or add a declarative definition,
then rerun the command.
