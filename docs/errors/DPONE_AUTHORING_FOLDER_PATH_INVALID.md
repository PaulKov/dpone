# DPONE_AUTHORING_FOLDER_PATH_INVALID

A fragment reference is not a relative POSIX `.yaml` or `.yml` path. Remove
absolute paths, `..`, backslashes, and non-YAML extensions. Paths are resolved
from the primary `pipeline.yaml` directory and must remain inside the project.
