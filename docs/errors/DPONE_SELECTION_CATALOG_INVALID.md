# DPONE_SELECTION_CATALOG_INVALID

dpone could not build one deterministic workload graph from the project domain
catalog and its explicit authoring sources. Fix the reported catalog/source
entry and rerun `dpone check .`. dpone does not publish a partial preview.
