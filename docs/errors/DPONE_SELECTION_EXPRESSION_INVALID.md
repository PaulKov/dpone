# DPONE_SELECTION_EXPRESSION_INVALID

The workload selector does not follow the bounded `method:value` grammar.
Run `dpone check --help`, correct the expression, and retry. Methods and values
are exact and case-sensitive; graph expansion uses only a leading or trailing
`+`.
