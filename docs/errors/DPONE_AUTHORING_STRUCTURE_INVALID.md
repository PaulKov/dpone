# DPONE_AUTHORING_STRUCTURE_INVALID

The chosen mode lacks its required structure. Classic requires `schemas`, flow
requires `processes`, and folder requires `fragments` without inline processes.
Validate the primary file against its public JSON Schema.
