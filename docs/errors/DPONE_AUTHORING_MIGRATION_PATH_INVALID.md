# DPONE_AUTHORING_MIGRATION_PATH_INVALID

The requested source or candidate path is missing, unsafe, outside the project
root, or traverses a symlink. Run the command from the project root and pass a
pipeline id, pipeline directory, or confined `pipeline.yaml`. Do not use
`--apply` until `--plan` succeeds.

See [authoring-mode migration](../airflow-authoring-migration.md).
