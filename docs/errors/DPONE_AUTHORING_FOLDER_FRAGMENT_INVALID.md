# DPONE_AUTHORING_FOLDER_FRAGMENT_INVALID

The fragment is not bounded UTF-8 YAML or violates its narrow contract. It must
contain only `kind: dpone.flow-fragment.v1` and a non-empty `processes` list.
Validate it against `etl-flow-fragment-manifest.schema.json`, then rerun check.
