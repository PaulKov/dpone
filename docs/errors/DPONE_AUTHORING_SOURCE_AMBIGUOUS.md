# DPONE_AUTHORING_SOURCE_AMBIGUOUS

The primary source contains both `processes` and `schemas`. Choose one authoring
mode: flow uses `processes`, classic uses `schemas`, and folder uses only an
explicit `fragments` list.
