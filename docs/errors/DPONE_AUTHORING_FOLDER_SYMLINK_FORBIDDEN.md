# DPONE_AUTHORING_FOLDER_SYMLINK_FORBIDDEN

A fragment path or one of its components is a symbolic link. Replace it with a
regular project-owned file below the configured root. Symlinks are rejected so
the compiled and pinned source graph cannot escape or change indirectly.
