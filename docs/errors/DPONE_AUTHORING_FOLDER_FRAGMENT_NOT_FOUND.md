# DPONE_AUTHORING_FOLDER_FRAGMENT_NOT_FOUND

An explicitly listed fragment is missing, not a regular file, or resolves
outside the project. Correct the project-relative POSIX path in `pipeline.yaml`
and rerun `dpone check <pipeline>`.
