# DPONE_ROUTE_FILTER_INVALID

A `dpone recipe list` source, sink, or strategy filter is unknown.

Run the unfiltered command:

```bash
dpone recipe list
```

Then reuse exact `source`, `sink`, and `strategy` values from the result.
`api` is accepted as an endpoint-family alias for the canonical `rest`
provider identity.
