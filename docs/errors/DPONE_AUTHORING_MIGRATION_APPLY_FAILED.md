# DPONE_AUTHORING_MIGRATION_APPLY_FAILED

The operating system or storage layer could not commit the guarded source
replacement. Inspect permissions and free space, verify the source in version
control, and rerun `--plan`. A newly created folder fragment is removed when
the primary source switch fails.

See [authoring-mode migration](../airflow-authoring-migration.md).
