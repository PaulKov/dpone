# DPONE_AUTHORING_SOURCE_INVALID

The `authoring` object is missing or invalid. It must contain one supported
`mode` and the exact project-relative primary `source` path. Use `dpone init
pipeline` to inspect a minimal valid example.
