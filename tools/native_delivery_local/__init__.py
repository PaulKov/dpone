"""Opt-in disposable ClickHouse to MSSQL native delivery test composition.

Importing this package never discovers credentials, connects to a service or
starts containers. The operator supplies the explicitly approved environment.
"""
