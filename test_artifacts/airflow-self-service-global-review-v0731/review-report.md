# Airflow self-service global review report

Status: **SHAPE-A RELEASED (`v0.73.2`)**

Reviewed base: `ef88ae7fbe678b748885eb52868604d5b653d02a` (`0.73.1`)

Released tip: annotated tag `v0.73.2` → `e297c6f9c0174f0c0637cd5b6c7f1de1849b2425`
(GitHub Release + PyPI publish
[run 29699984679](https://github.com/PaulKov/dpone/actions/runs/29699984679)).
Hardening landed via PR [#347](https://github.com/PaulKov/dpone/pull/347); release
path hotfixes [#348](https://github.com/PaulKov/dpone/pull/348)–[#352](https://github.com/PaulKov/dpone/pull/352).

SS-46 atomic v1→v2 production cutover and SS-47 **external** privileged
controller remain **explicitly deferred**. ADR 0028 / release-trust v2 is **not**
claimed active. Live K8s/Vault/GCS inventories stay `UNVERIFIED`.

Airflow UX P2 is **IMPLEMENTED** ([#353](https://github.com/PaulKov/dpone/pull/353),
`docs/feature-design-airflow-pipeline-first-ux-p2.md`). Shape B provisioning
progress (see `shape-b-live-inventory-2026-07-19.md`): tag ruleset
`protect-release-tags` (`19179508`); GitHub App `4341356` / installation
`147673155`; B2 Object Lock smoke PASS. Full bootstrap mutation path is live:
lease → Snapshot A → draft → AUTHORIZED → LEASE_RELEASED (controller run
`29722846127`, no PASS/GO). **Still blocked for activation:** Trusted Publisher
rebind, draft publish, environment protection enforcement, atomic policy v2
cutover. ADR 0028 is **not** claimed active.

This report covers the beginner CLI, canonical authoring path, preview and safe
sample construction, Airflow deployment index/provider, quality gates,
credential-safe output, filesystem transactions, documentation, packaging, and
required evidence. It excludes the user-owned `.codex/config.toml`.

## Findings and remediation

| ID | Severity | Finding | Remediation status |
|---|---|---|---|
| SS-01 | High | Index-backed DAGs could resolve raw/external pack paths outside the deployment cache. | Fixed locally: deployment-index nodes require `cached://` workload `pack_ref`; raw `pack_path` rejected (`DPONE_CACHE_REF_INVALID`). Parse-hardening suite PASS. |
| SS-02 | High | Diagnostic invalid-DAG handling could overwrite an existing real DAG; duplicate precedence was unsafe. | Fixed locally: collision decision before mutation; occupied diagnostics are report-only. Parse-hardening suite PASS. |
| SS-03 | High | Cached pack/index limits were applied after unbounded reads or hashing. | Fixed locally: index/pack byte ceilings enforced before full reads/hashes. Parse-hardening suite PASS. |
| SS-04 | High | The parse SLO fixture loaded zero valid DAGs and bypassed the runnable `init_fetch` path. | Fixed locally: fingerprinted cache-only runnable fixture; parse SLO suite PASS. |
| SS-05 | High | `workload init --apply` could follow catalog symlinks and ignore a raced scaffold conflict. | Fixed locally: confined no-follow catalog CAS; scaffold conflict stops before catalog writes. Focused workload-init suites PASS. |
| SS-06 | High | Primary authoring bytes were not pinned through check, pack, release, and safe-sample promotion. | Fixed locally: one confined authoring snapshot + digest parity through check/pack/preview/safe-sample; drift fails closed. Snapshot-pinning suite PASS. |
| SS-07 | High | `dpone fix --apply` had a check-then-replace race. | Fixed locally: no-clobber quarantine/install protocol preserves a concurrent winner and recovery bytes. |
| SS-08 | High | Scaffold rollback could unlink a late concurrent winner. | Fixed locally: rollback removes only the quarantined inode/content owned by the operation. |
| SS-09 | High | URI userinfo, YAML block scalar secrets, and `vault_path` could cross public output boundaries. | Fixed locally: URI/YAML redaction already green; `vault_path` added to shared forbidden/sensitive key sets so scaffold diffs and `redact_public_value` fail closed. Focused redaction suites PASS. |
| SS-10 | Medium | Pipeline source resolution differed by command and used unsafe basename fallback. | Fixed locally: one lexical resolver; bare IDs map only to `pipelines/<id>/pipeline.yaml`. |
| SS-11 | Medium | Absolute workstation paths escaped into explain/check/release diagnostics. | Partially fixed; snapshot worker is completing release and safe-sample parity. |
| SS-12 | Medium | A repository FIFO could block confined authoring reads before file-type validation. | Fixed locally: POSIX reads use `O_NONBLOCK` and reject non-regular files. |
| SS-13 | High | Root quality policy and `checks` compatibility could be dropped or become false success. | Fixed: strict normalization, root propagation, probe-unavailable failure, and structured gate evidence. |
| SS-14 | High | Missing Kubernetes provider and dependency-light fallback behavior could produce fake execution. | Fixed locally: Airflow core without cncf KPO fails at construct; dependency-light fallback may construct for unit isolation but execute/reentry fail closed. Focused step-visibility suite PASS. |
| SS-15 | Medium | Beginner installation, provider API, cache recovery, quality examples, and runtime-image docs contradicted shipped contracts. | In progress: phase/persona-scoped documentation hardening. |
| SS-16 | Architectural limitation | Legacy MSSQL sinks still run quality after final target mutation when no staged-load port exists. | Explicit follow-up. A cross-sink transaction redesign is outside this bug-review contract and requires an approved feature specification. |
| SS-17 | Critical | Runnable `init_fetch` was declared by the index but provider KPOs still executed scheduler-embedded commands and never invoked a verified runtime fetch/launcher path. | Fixed locally for the unit/provider path: v2 tripwire, parse preflight, fixed KPO cmds/mounts, bounded plan, receipt/ready-last, shell-free launcher, plus closed adjacent SS-14/27/28/29. Live K8s/attestation remains `UNVERIFIED`. |
| SS-18 | High | Strict v2 delivery validation leaked into safe-sample v1 and silently changed a previously runnable pinned local fetch into `init_fetch: null`. | Fixed locally: safe-sample keeps soft v1 init_fetch completeness; legacy delivery without `trust_tier`/`registry_config_ref` stays runnable; verified v2 current fails with `DPONE_SAFE_SAMPLE_EXECUTABLE_INDEX_UNSUPPORTED` instead of silent `init_fetch: null`. Focused regression tests added; live path remains `UNVERIFIED`. |
| SS-19 | Medium | Public recursive path redaction emitted `$ABSOLUTE_PATH`, but the connection-check output schema rejected that stable public placeholder. | Fixed in the schema producer; generated reference regeneration and schema test are pending final integration. |
| SS-20 | Medium | The new runtime-image CLI imported a runtime exception directly and violated the command-layer dependency rule. | Fixed: readiness facade owns the public runtime-delivery error alias; import-rules gate passes. |
| SS-21 | Medium | A service-level concurrency test still injected the retired unlink/link algorithm and no longer exercised ADR 0023 atomic-exchange recovery. | Fixed: the test now injects two writers around the certified exchange/rollback boundary and proves winner preservation plus bounded recovery output. |
| SS-22 | High | Deployment reuse compared parsed JSON values instead of exact expected bytes and checksums, so reformatted or replaced files could be accepted under stale descriptors. | Fixed locally: reuse compares exact published bytes via confined leaf reads; reformatted JSON fails closed. Deployment-artifacts suite PASS. |
| SS-23 | High | Deployment source reads had symlink/TOCTOU, unbounded-read and environment path-confinement gaps. | Fixed locally: no-follow confined readers, pre-read size ceilings, environment segment normalization, TOCTOU fstat checks. Deployment-artifacts suite PASS. |
| SS-24 | High | A typed passing `QualityGateReceipt` was not bound to the current normalized gate policy; an empty, stale or unrelated report could suppress authored gates. | Fixed locally: one immutable gate/acceptance snapshot and locked process-local execution authority bind policy, run, load, boundary, object identity and consumption order; drift/replay/replacement fail before mutation, checkpoint, evidence or source-state acceptance. The ADR explicitly does not claim durable or cross-process authority. |
| SS-25 | High | Lazy native transfer quality used range-width estimates as exact source rows before observing sparse physical output. | Superseded/fixed by SS-76: lazy scope stays unavailable before export evidence; source uses `slice_evidence.rows_exported`, target uses staged/loader counts; estimates never certify. |
| SS-76 | High | Native quality could compare a sink loader count with the same value copied into the source side, or coerce malformed loader/export counts with `int(...)`, creating false passed receipts. | Fixed locally: `native_transfer_row_authority` publishes independent authorities, checkpoints gain additive `rows_loaded` + `row_count_authority` only when both validate, and old unmarked checkpoints fail closed for quality. Focused 72-test slice PASS; live MSSQL→ClickHouse remains `UNVERIFIED`. |
| SS-26 | High | A partially successful checkpoint batch write could leave a target-committed partition unrecorded in the current terminal state, allowing later failure handling to downgrade it. | Fixed locally: exact current-attempt durable commits are reconciled before the original store error is propagated; a fresh reviewer is active. |
| SS-27 | High | Strict provider execution still accepted top-level Airflow execution controls and broad operator overrides that could bypass pack-authoritative identity, placement and trigger ordering. | Fixed locally: strict path rejects non-asset `airflow.execution` knobs and all `operator_overrides`; authority suite PASS. |
| SS-28 | High | Strict XCom execution ignored the declared sidecar image, used a mutable default and did not reject unmodeled connection projection. | Fixed locally: digest-pinned sidecar + final-pod validation remain; mutable Alpine default removed — XCom push requires explicit sidecar image (`DPONE_AIRFLOW_XCOM_SIDECAR_IMAGE_REQUIRED`). |
| SS-29 | High | `pack_fingerprint` was self-declared and its producer payload omitted execution-bearing fields, so provider/runtime mutations could retain the same identity. | Fixed locally: producer/build/provider/runtime recompute via `pack_identity`; dead alternate `compact_pack_fingerprint` helper removed. |
| SS-30 | Medium | Post-commit quality failures discarded authoritative load counters and surfaced synthetic zero rows/attempts. | Fixed locally: only typed mutation-owner outcomes enrich the failed result; legacy constructors do not invent stronger claims. |
| SS-31 | Medium | Native logical quality scope existed only inside the receipt and was missing from durable reconciliation/runtime report evidence. | Partially fixed: one bounded canonical evidence parser and runtime-report projection are green; coordinator persistence is included in receipt remediation. |
| SS-32 | High | Release publication previously lacked a single exact-commit gate combining annotated-tag identity, synchronized package bytes and live required-check producer provenance. | Fixed locally: fail-closed release identity and live exact-SHA gates are wired into the release preflight. |
| SS-33 | Medium | The dry-run execution plan still emitted deprecated `quality.checks: []` when no quality policy was authored, teaching a different DSL than generated manifests and runtime. | Fixed locally: the output-only default is canonical `quality.gates: []`; compatibility input remains accepted only by the normalizer. |
| SS-34 | Medium | The documentation homepage changed the executable `Latest release vX.Y.Z` identity marker to an unrecognized candidate label and broke release smoke. | Fixed locally without weakening the release contract; docs now expose the synchronized project version. |
| SS-35 | High | The verified runtime launcher accepted kind-confused commands, duplicate `--phase`, and a phase value that did not match the verified pack; receipt JSON also admitted ambiguous/non-finite values. | Fixed locally: exact runtime/pre-hook grammars bind selectors and hook IDs, the only accepted environment is canonical, and receipt parsing rejects duplicate keys, literal non-finite constants, exponent overflow and recursion failures. |
| SS-36 | High | Deployment build/provider input accepted duplicate logical IDs, duplicate JSON keys, non-finite numbers, and a runnable v1 current activation without an environment. | Fixed locally: build and provider reject duplicate identities before artifact reads, strict index/DAG decoding rejects recursion and all non-finite forms, and `current` v1 activation requires an exact environment while direct pinned v1 compatibility remains unchanged. |
| SS-37 | Critical | A legacy commit status could satisfy a required release context without the configured GitHub App producer, and the live required-context set was not bound to the checked-in policy. | Fixed locally: statuses are diagnostic only; each required context needs a positive live GitHub App ID and a same-app check-run, while context names and ruleset ID must exactly match checked-in policy. Live GitHub execution remains CI-owned and `UNVERIFIED` locally. |
| SS-38 | High | The pre-publish package gate did not treat `dist/` as a closed set and could accept duplicate wheels/sdists, `.zip`, stray files, directories, or symlinks. | Fixed locally: deterministic inventory-only gate requires exactly eight artifacts before archive inspection, attestation, or publication. |
| SS-39 | Critical | Runtime-image publication treated every manifest lookup failure as absence and pushed mutable tags before digest smoke, SBOM, and attestation verification; reruns and concurrent `latest` promotion were not safe. | Fixed locally: digest-only build, bounded authenticated lookup classification, receipt-derived certification, verified attestations, retry-safe alias reconciliation and partial-publication evidence are implemented. Focused 93-test integration gate passes; fresh review is active and live GHCR/Docker remains `UNVERIFIED`. |
| SS-40 | High | Tenant-specific GCS/Vault/Mindbox defaults remained active runtime policy, while the package archive gate inspected member names but not bounded text bodies for private infrastructure material. | Fixed locally: active `example-dp-*` GCS/Vault templates and Mindbox `endpoint_id` defaults removed (fail closed with `DPONE_LEGACY_RUNTIME_DEFAULTS_DISABLED` / explicit `endpoint_id`); `tenant_hygiene` source/archive body gate + `ResolvedConnectionDescriptor` green; release preflight/build invoke hygiene via CI-owned `TENANT_HYGIENE_POLICY`. Residual: test/CI fixture literals outside `src/` may still mention tenant hosts; live Vault/GCS remains `UNVERIFIED`. |
| SS-41 | Medium | Public docs and generated examples used `quality checks` after `quality.gates` became the canonical DSL, so prose tests did not protect the user-facing terminology. | Fixed locally: public prose and scaffold/catalog text use `quality gates`; a regression test protects entrypoints and every source-sink page while preserving literal compatibility fields. |
| SS-42 | Medium | Beginner docs presented the fifth safe-sample command as an unconditional offline success even though a correct unconfigured runtime exits `3` without I/O. | Fixed locally: the four-command offline preview is the normal success path; safe sample is an optional platform-gated fifth command and end-to-end usability evidence requires exit `0`. |
| SS-43 | High | Release identity read version/changelog metadata from the mutable worktree while claiming to certify a frozen commit. | Fixed locally: package and changelog bytes are read strictly from `git show <commit>:<path>`; dirty and invalid worktree bytes cannot influence the result. |
| SS-44 | High | Exact internal dependency parsing accepted a conditional marker such as an always-false `python_version` expression as a valid mandatory pin. | Fixed locally: every required internal edge rejects markers, URLs, extras, duplicate declarations and non-exact specifiers. Focused release-identity tests pass. |
| SS-45 | High | The exact-commit gate read branch-protection policy from the mutable worktree and emitted no committed policy digest. | Fixed locally: policy loads only via `git show <commit>:.agents/policy/github-branch-protection.yml`, evidence binds `policy_sha256` + fixed `policy_path`, and alternate `--policy` paths fail closed. Focused release-commit-gate suite PASS. |
| SS-46 | High | The live ruleset comparison omitted target/conditions/bypass actors and committed producer IDs/workflow identity; a changed live producer or tag-target ruleset could still return GO. | Wired: frozen v2 → projection; live ruleset normalizer; evaluate/drift SS-46 blockers; A/B dual-read helper. Production policy still v1 so live path keeps legacy compare until atomic cutover + inventory. |
| SS-47 | High | Privileged release jobs executed checked-out repository code while holding attestation, OIDC, signing-key or contents-write authority. | Candidate workflows hardened: `release.yml` `attest` / `publish` / `github-release` and `runtime-image.yml` `push-attest-runtime-candidate` / `promote-certified-image` have zero checkout; build/certify stay read-only; SS-47 detector scans both workflows and fails closed. External controller composition root still required for full ADR 0028 closure. |
| SS-48 | High | Annotated tag identity was checked only at preflight; tag deletion or movement before PyPI/GitHub publication made evidence stale. | Fixed locally for pre-publication **annotated-tag peel** rechecks: PyPI `publish` and `github-release` re-read tag object/peeled commit via GitHub API without checkout (not a full `release_identity_gate` re-run). Full immutable-tag ruleset + privileged controller remain under approved ADR 0028 cutover. Live release run remains `UNVERIFIED`. |
| SS-49 | High | Closed package inventory rejected symlink children but followed a symlink used as the `dist/` root and lacked fd-bound change detection while hashing. | Fixed locally: no-follow root/entry descriptors, before/after identity checks, replacement detection and exact closed-set validation. Secure inventory is split from the CLI; 16 focused tests, Ruff and mypy pass. |
| SS-50 | Medium | Release polling and candidate inventory described bounds without enforcing maximum timeout, interval, directory entries, or artifact bytes. | Fixed locally: timeout, poll interval, entry count, per-file and aggregate byte ceilings fail before network/hash work. |
| SS-51 | Medium | Standalone release identity allowed a caller-selected `HEAD`/commit as protected base, turning ancestry into a tautology. | Fixed locally: protected base is `origin/<branch>` from frozen policy only; mismatched `--remote-ref` fails closed; evidence binds `protected_base_sha` and `policy_sha256`. Focused release-identity suite PASS. |
| SS-52 | High | The API runtime registry forwarded one `vault_mount_point` argument to incompatible provider `from_vault` methods; dummy `**kwargs` tests masked real built-in factory failures. | Fixed locally for the legacy factory path: `from_vault` is now invoked only with signature-accepted parameters (no blind `vault_mount_point` / `**kwargs` forward). Canonical path remains `build_api_runtime_source_from_connection`. Focused registry contract tests PASS. |
| SS-53 | High | Vault paths, GCS bucket/URI topology, proxy locators, response text and raw backend exceptions could cross logs or evidence. | Fixed locally for release/public surfaces: `vault_path` redaction, `ResolvedConnectionDescriptor` non-secret metadata, and CI-owned tenant hygiene body scans are green. Residual: not every connector log site is exhaustively sentinel-tested; live Vault/GCS evidence remains `UNVERIFIED`. |
| SS-54 | Critical | Some GCS replacement routes deleted an active prefix before a replacement export was validated, and BigQuery staging selected/inferred GCS only after extraction with random object identity. | Fixed locally: attempt-scoped GCS identity before I/O, no pre-export delete of active prefixes, BigQuery commit then cleanup-debt + retire prior generation (`gcs_replacement` + `finalize_after_commit`). Debt-record failures raise `CommitOutcomeUnknownError`; retire-after-debt client failures warn only. Focused `tests/test_gcs_replacement.py` PASS (10). Residual: create-only upload/digest replay in native ClickHouse export and live GCS/BQ remain `UNVERIFIED`. |
| SS-55 | Critical | Indexed-v2 KPO carried binding/registry/runtime fingerprints but dropped the three exact credential-context artifacts before init-fetch; `dpone run` then built source, sink, API and state connectors from legacy `connection_id`, `connection_type` and `vault_path`. Canonical-only manifests fail after successful init-fetch, while dual authority can connect to the wrong endpoint. | Fixed locally for the composition-root gate: six-artifact handoff, verified context loader, strict hydrator resolution, conflict rejection, and default-disabled legacy policy (SS-73) are green; e2e pinned-context→hydrator test PASS. Live Kubernetes/Vault remains `UNVERIFIED`. |
| SS-73 | High | Legacy runtime connection authority remained available without an explicit one-release migration policy. | Fixed locally: only `runtime.compatibility.legacy_runtime_connections: explicit_only` with authored legacy fields permits the path; ambient env cannot enable it. |
| SS-56 | Critical | Nested normalization evaluated one single-use quality authority once per root/child table. The initial patch moved to one aggregate evaluation, but the claimed package transaction still performs sequential target commits, uses incomparable root/child quality counts, can advance snapshots on terminal failure, loses member evidence and appends same-load spill rows. | Fixed locally for the fail-closed staged package path: production nested loads require `stage_payload`/`finalize_staged_load`/`abort_staged_load`, stage all members, evaluate package quality once, then finalize-all or abort-all. Adversarial unit coverage added. Residual risk: sinks that cannot unfinalize a partially finalized member set; live nested routes remain `UNVERIFIED`. |
| SS-57 | High | Parse evidence could report PASS when process-cold import plus first load took 99 seconds, when matrix identity was `N/A`, or while raw worker/setup errors exposed secret-bearing text. | Superseded/fixed by SS-67 local certification integrity remediation; parse-benchmark/SLO suites PASS. Installed-wheel matrix remains `UNVERIFIED`. |
| SS-58 | High | Safe-sample runtime could return `execution_status=succeeded` and CLI exit `0` for failed/blocked copy results with missing structured errors or contradictory row/byte counters. | Superseded/fixed by SS-64 local envelope + auth remediation; docs JSON schemas regenerated to match Python producers. Live MSSQL/ClickHouse remains `UNVERIFIED`. |
| SS-59 | High | A failed or throwing quality evaluation left its single-use authority reusable, permitting a second runner invocation against the same mutation boundary. | Fixed locally: terminal `FAILED`/`INVALID` states are entered before returning/raising and concurrent evaluators invoke the runner exactly once. The state machine remains in a 269-SLOC module after responsibility-based extraction; 46 focused and adjacent quality tests pass. |
| SS-60 | High | The public `provider_execution` schema diverged from runtime validation, and valid 64–128 character workload IDs were copied into invalid Kubernetes labels. | Fixed locally: one schema authority is shared by producer/provider; logical IDs remain unchanged in artifacts/evidence while bounded deterministic Kubernetes projections and full-ID annotations are used for pod metadata. 166 focused tests pass. |
| SS-61 | High | If READY link publication committed but directory fsync failed, cleanup could delete the complete authenticated worktree; after READY, the launcher also trusted mutable extracted files without re-verifying the full tree. | Fixed locally: ambiguous committed publication preserves the worktree for retry, pre-commit failures still clean up, and the launcher re-verifies the complete no-follow tree against the archive before execution. KPO mounts are asserted read-only. |
| SS-62 | High | Broad Airflow 3-to-2 import fallback swallowed transitive `ModuleNotFoundError`, generic `ImportError` and SDK attribute failures, masking broken Airflow 3 installations as compatibility fallback. | Fixed locally: fallback occurs only when the exact requested Airflow 3 module is absent; transitive and generic import failures propagate. 20 focused tests pass. |
| SS-63 | High | Runtime-image certification was manually synthesized as `status: PASS` from caller-declared check IDs rather than derived from command receipts, and its output location conflicted with the producer's Docker-context boundary. | Fixed locally: nine digest/source-bound receipts are written only after real pinned commands, `certify` validates checks/SBOM/attestations/context, and evidence is written outside the build context. Focused workflow/policy suite has 93 PASS; fresh security review remains active. |
| SS-64 | High | Safe-sample live authorization accepted non-temporary, cleanup-disabled and unmasked target plans; malformed success envelopes, production-shaped diagnostic mismatches and contradictory quarantine counts could still exit zero. | Fixed locally: live authorizer + envelope validation + CLI exit mapping green; docs `safe-sample-data-copy` / `safe-sample-runtime-execution` schemas synced from Python producers (phase1b schema suite PASS). Live route remains `UNVERIFIED`. |
| SS-65 | High | PyPI release smoke bound GO to an initial candidate inventory but did not revalidate the complete filename/digest set after long network checks, so an archive replacement could still return exit zero. | Fixed locally: inventory → network verify → final exact filename/size/SHA-256 revalidation; mutation probes fail closed. Focused PyPI smoke suites PASS; live PyPI remains `UNVERIFIED`. |
| SS-66 | High | Runtime-image `latest` could preserve an uncertified or tag-raced digest, alias creation could wrap a manifest into a new digest, and successful partial publication had no durable per-transition journal. | Fixed locally: digest-pinned inspection, fail-closed certification proof, exact-manifest aliasing, and durable publication journal. Focused runtime-image registry/promotion/certification suites PASS; live GHCR/Docker remains `UNVERIFIED`. |
| SS-67 | High | Airflow parse certification used an invalid strict-v2 fixture, started its cold clock after material provider imports, did not bind exact installed wheels/source commit, and accepted stable but wrong DAG topology. | Fixed locally: sterile process-cold worker, valid six-artifact fixture, exact candidate manifest, and topology authority. Focused parse-benchmark/SLO suites PASS; installed-wheel matrix remains `UNVERIFIED`. |
| SS-68 | High | The proposed release-trust architecture lacked a safe migration for the shared governance policy, final governance snapshot, enforceable privileged composition root, pre-mutation serialization, replay identities and schema-complete append-only evidence. | Closed at design level: architect `APPROVE_SPEC` + maintainer `APPROVED` / ADR 0028 Accepted (2026-07-19). Non-live schemas + parser/projection prep landed; atomic v1→v2 policy/consumer cutover and live inventories remain open. |

## Evidence collected so far

| Check | Status | Evidence |
|---|---|---|
| CLI init option-order and generated reference tests | PASS | `tests/test_init_cli_contract.py`, `tests/test_cli_reference_service.py` |
| Confined FIFO tests | PASS | `tests/test_confined_file_safety.py` |
| Authoring filesystem transaction tests | PASS | `tests/test_authoring_filesystem_transactions.py` |
| Pipeline source resolution tests | PASS | `tests/test_pipeline_source_resolution.py` |
| Explain/public path tests | PASS | `tests/test_airflow_explain_public_paths.py`, `tests/test_airflow_self_service_public_paths.py` |
| Focused provider baseline | PARTIAL | Existing focused tests pass except the confirmed invalid SLO fixture; remediation is active. |
| Strict runtime init-fetch focused suite | PASS | 105 tests across `test_init_fetch_trust_boundary.py`, `test_airflow_runtime_phase1b_contracts.py`, and `test_airflow_runtime_init_fetch_cli.py`; four additional archive/ready security tests pass. |
| Quality checks-to-gates compatibility and acceptance | PASS | 233 tests across quality adapter, runtime acceptance, schema, load-governance, and finalization suites. |
| Sparse native quality scope | PASS | `tests/test_native_transfer_quality_scope_actual_rows.py` plus adjacent native runtime/processor suites. |
| Independent native row authorities (SS-76) | PASS (local) | `tests/test_native_transfer_row_authority.py`, updated quality-scope/runtime/ETL resume suites; Ruff, scoped mypy, and import-rules PASS. Live route `UNVERIFIED`. |
| Native quality-scope evidence boundary | PASS | `tests/test_native_transfer_quality_evidence.py`; 7 tests plus Ruff and scoped mypy. |
| Partial checkpoint batch failure | PASS (local) | `tests/test_native_transfer_checkpoint_partial_batch_failure.py` plus native runtime/processor suites; 31 tests. Fresh review pending. |
| Post-commit quality counters | PASS | `tests/test_etl_processor_post_commit_failure_state.py` plus adjacent processor/CLI suites; 20 tests. |
| Release identity and exact-commit trust gate | PASS (local) | SS-45/SS-48/SS-51: frozen-policy digest, pre-publication tag recheck, protected base from policy (`protected_base_sha`/`policy_sha256`); `tests/agent_policy/test_release_identity_gate.py`, `test_release_commit_gate.py`, `tests/test_release_smoke_contracts.py` PASS. Live GitHub invocation remains CI-owned. |
| Closed PyPI candidate inventory | PASS (local) | SS-65 final exact-set revalidation after network checks; replacement/addition/removal probes fail closed. Live PyPI remains `UNVERIFIED`. |
| Runtime-image certification and promotion | PASS (local) | SS-66 registry/promotion/certification suites PASS; live GHCR/Docker remains `UNVERIFIED`. |
| Airflow parse false-PASS regressions | PASS (local) | SS-67 parse-benchmark/SLO suites PASS; installed Airflow wheel matrix remains `UNVERIFIED`. |
| Safe-sample result invariants | PASS (local) | SS-64 authorization/envelope/CLI suites green; schema producers match generated docs JSON. Live MSSQL/ClickHouse remains `UNVERIFIED`. |
| Quality execution evaluate-once authority | PASS (local) | 46 focused tests plus adjacent quality/finalization/native/nested suites; terminal failures cannot be retried and module-size hard gate passes. |
| Provider schema and Kubernetes identity projection | PASS (local) | 166 focused tests plus Ruff and scoped mypy; producer/provider schema authority and bounded Kubernetes label/annotation projection are shared. |
| Runtime init-fetch committed-state/tree integrity | PASS (local) | 58 READY-publication tests and 94 adjacent launcher/provider tests; ambiguous fsync preserves committed worktree, complete tree is reverified, and base mount is read-only. |
| Airflow 2/3 exact import fallback | PASS (local) | 20 focused tests prove only the exact missing Airflow 3 module triggers compatibility fallback. |
| Production KPO credential authority | PASS (local) | SS-55/SS-73: six-artifact handoff, verified context loader, strict hydrator, default-disabled legacy policy, and pinned-context→hydrator e2e PASS. Live Kubernetes/Vault remains `UNVERIFIED`. |
| Nested aggregate quality transaction | PASS (local) | SS-56 staged package path: require abort port, stage-all → package quality → finalize-all/abort-all; partial finalize and abort failures fail closed; `NestedLoadService` requires `package_mutation` (no free committing `load_one`). Residual: already-finalized members cannot be unfinalized by the sink port; live nested routes remain `UNVERIFIED`. |
| GCS replacement commit-then-retire (SS-54) | PASS (local) | `tests/test_gcs_replacement.py` (10); debt fail-closed + no pre-export delete. Live GCS/BQ `UNVERIFIED`. |
| Tenant hygiene + neutral defaults (SS-40) | PASS (local) | Hygiene suites + GCS/Mindbox fail-closed defaults; release workflow wired to `TENANT_HYGIENE_POLICY`. |
| Exact-commit frozen policy (SS-45) | PASS (local) | `tests/agent_policy/test_release_commit_gate.py`; `policy_sha256` bound. |
| Managed plan canonical quality output | PASS | `tests/test_managed_ux_contracts.py`; 14 tests, including the loopback Studio contract outside the filesystem sandbox. |
| Release/docs version identity | PASS | `tests/test_docs_language_contracts.py`, `tests/test_release_smoke_contracts.py`, and `tests/test_oss_release_readiness.py`; 43 tests. |
| Import rules and layer metrics | PASS | No import violations; layer metrics remain within baseline regression budgets. |
| Module-size hard gate | PASS | New runtime orchestrator was split below the warning threshold; no new module-size debt. |
| Safe-sample compatibility | PASS (local) | Legacy v1 init_fetch without strict v2 fields stays runnable; executable v2 indexes fail loud with `DPONE_SAFE_SAMPLE_EXECUTABLE_INDEX_UNSUPPORTED`. Live safe-sample remains `UNVERIFIED`. |
| Live Airflow/Kubernetes/Vault/MSSQL/ClickHouse | UNVERIFIED | No approved live environment or credentials were supplied. |
| Broad non-live, docs, package, and strict MkDocs gates | PASS (local) | Pre-PR local: ruff/mypy/import-rules/layer-metrics/module-size; `pytest -m "not integration_live" -n auto` ~6910 passed / 476 skipped; docs + `mkdocs build --strict`; wheels `0.73.2` + twine. |
| PR #347 required checks on tip `f4a7b63e` | PASS (CI) | Quality 3.11/3.12; full Airflow 2.10–3.3 × py3.11/3.12 matrix + parse SLO evidence; runtime-image PR smoke; CodeQL; backfill matrix; docs build; TruffleHog; Dependency Review; Analyze Python. Observed 2026-07-19. |
| Runtime connection authority design | PASS | `docs/feature-design-runtime-connection-authority-and-hygiene-v0732.md` is APPROVED and ADR 0027 is accepted. Implementation and live certification remain pending. |
| Release trust architecture design | PASS (design) | Feature `APPROVED` and ADR 0028 Accepted (2026-07-19). Cutover contract validates PASS. v2 schemas + parser/projection prep landed; atomic production policy migration and live inventories remain open. |
| Governance policy v2 non-live prep | PASS (local) | `test_governance_policy_v2`, `test_governance_snapshot`, `test_release_privileged_boundary`; gate/drift v2 projection wiring; production policy remains v1. |

## Merge and release decision

Current verdict: **shape A `v0.73.2` RELEASED** (PyPI + GitHub Release PASS on
tag tip `e297c6f9`). SS-46 atomic cutover and SS-47 external controller remain
deferred by contract. Live K8s/Vault/GCS inventories stay `UNVERIFIED`.

### Merge / tag checklist (shape A)

| Step | Status | Owner |
|---|---|---|
| Tip `f4a7b63e` PR #347 required checks green | ✅ PASS | CI |
| CHANGELOG documents deferred SS-46/47 + non-activation of release-trust v2 | ✅ | Integrator |
| Review report points at final tip + CI evidence | ✅ | Integrator |
| Maintainer approving review on PR #347 | ✅ (owner GO) | Maintainer |
| Merge PR #347 to `master` | ✅ | Maintainer |
| Post-merge tip SHA recorded; release hotfixes #348–#352 | ✅ | Release owner |
| Explicit **tag GO** for shape A `0.73.2` | ✅ | Maintainer |
| Create annotated tag `v0.73.2` / run release workflow | ✅ PASS ([29699984679](https://github.com/PaulKov/dpone/actions/runs/29699984679)) | Release owner |
| Claim ADR 0028 / policy v2 / external controller active | ❌ forbidden in shape A | — |
| Fabricate live GitHub/PyPI/K8s/Vault/GCS IDs | ❌ forbidden | — |

### When a release makes sense

| Release shape | Ready when | Not ready while |
|---|---|---|
| **A. Patch of tactical remediations** as `0.73.2` | ✅ **SHIPPED** | — |
| **B. Full release-trust boundary** (ADR 0028 activated) | Live inventory of ruleset/version/bypass/producer/controller/App/store/Trusted Publisher IDs; atomic policy/parser/consumer cutover; external controller provisioned; A/B/C rehearsal evidence; no privileged candidate mutation path; fresh GO review | Production policy still v1; placeholders; missing controller |
| **C. Ordinary feature / docs patch** (e.g. Airflow UX P2) | ✅ **SHIPPED** ([#353](https://github.com/PaulKov/dpone/pull/353)) | — |

**Recommendation now:** keep ADR 0028 / release-trust v2 **inactive**. Next
shape B work is App + protected mutation environments + evidence store, then
Trusted Publisher rebind and atomic SS-46/47 cutover under a fresh GO review.
Do not treat the controller scaffold as publication authority.

Missing live infrastructure remains `UNVERIFIED`, never `PASS`.
