# dpone 0.2.0 docs and release verification artifact

- Artifact id: 20260603T153554+0300_dpone_0_2_0_docs_release
- Date: 2026-06-03 15:35:54 +0300
- Operator: Codex
- Repository path: <workspace>/dpone
- Branch: codex/yandex-webmaster-landing-family
- Target package version: 0.2.0

## Scope

- Updated the public README so the documentation map is near the top of the file.
- Added professional GitHub Pages documentation infrastructure with MkDocs Material.
- Added strict Pages CI workflow for docs builds and deployment from `master`.
- Updated architecture documentation for the production runtime connector architecture.
- Added ADR 0005 for production runtime connector rules.
- Kept package release flow on GitHub Actions Trusted Publishing for PyPI.

## Verification results

| Check | Result |
| --- | --- |
| `uv run ruff check .` | passed |
| `uv run ruff format --check .` | passed |
| `uv run mypy --config-file mypy.ini` | passed |
| `uv run pytest -m "not integration_live" -q` | passed |
| `uv run --with mkdocs --with mkdocs-material --with pymdown-extensions mkdocs build --strict` | passed |
| `uv build` | passed, built `dist/dpone-0.2.0.tar.gz` and `dist/dpone-0.2.0-py3-none-any.whl` |
| `uvx twine check dist/*` | passed |
| PyPI version check | latest is `0.1.0`; `0.2.0` is not published yet |
| Local `PYPI_API_TOKEN` | missing in this shell |
| Local `GITHUB_TOKEN` | missing in this shell |
| Release workflow | `.github/workflows/release.yml` supports Trusted Publishing on tag push |

## Publication note

Local token-based PyPI upload is not possible from this shell because `PYPI_API_TOKEN` is not present. The repository release workflow is configured to publish on `v*.*.*` tags using PyPI Trusted Publishing, so pushing tag `v0.2.0` is the intended release mechanism.
