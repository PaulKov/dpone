# Benchmark identity and comparability corrections

Status: APPROVED under the maintainer's instruction to correct the two independently
reproduced PR34 P2 findings. The root integrator is the sole writer. The independent
architecture reviewer approved this bounded contract on 2026-09-11. This supplements
the existing approved delivery specification; original specifications and evidence
remain unchanged. No delivery, manifest, journal or checkpoint migration is required.

Reviewed source is `fa16c23c137c76fc4b218cd729820b987b698418`; the actual benchmark
baseline remains `d5ad9aaecc900c24df421b160ed36b4cfc726e45`. Adopted upstream is
`a1aadcaf6beaa2e82abcac27f2aed0b5113986f7`, including the preceding PostgreSQL
campaign closure and release 0.77.1. Import both commits with provenance, preserve
all 16 upstream-only modes/blobs, and resolve only the shared changelog by retaining
delivery changes under Unreleased and the complete upstream 0.77.1 release section.
Versions, dependency pins, lockfile, policy and foreign tests are import-only paths.

## Corrections and algorithm

1. The offline reader validates exact limit fields and values using the existing
   canonical model. Hash its normalized limit record and compare it with the
   recorded configuration digest. Hash the complete environment except its outer
   sha256 and compare that with the recorded digest. Reject either mismatch before
   accepting receipts or samples. Preserve invalid_limits and existing sanitized
   exception/CLI boundaries; distinguish configuration_digest_mismatch and
   environment_digest_mismatch. Keep all existing retained-byte and receipt checks.
2. Add one pure environment comparison projection beside the existing envelope
   contract. Omit only the outer digest and mask the value of an existing
   versions.dpone entry. Preserve that key's presence, every other version, layout,
   resource value/type and field. Canonical JSON equality remains authoritative.
   Never alter the original report or its full per-subject digest and bindings.
3. Reuse this policy in both comparison entrypoints. The harness imports it only
   inside require_comparable, which runs in the candidate installation. That helper
   also verifies each environment digest before projection. Keep its
   invalid_run_contract error. Preserve baseline-compatible run, inspect and help
   imports and runner.configuration's adaptation to the actually loaded old model.

Valid v1 producer evidence requires no migration. Stale/corrupt evidence must be
regenerated by its producer. Within-run environment checks still include dpone's
version; only comparisons across independently bound subjects allow it to differ.
Checksums detect content inconsistency, not authenticated live execution.

## Definition of done

- Red regressions reproduce stale configuration/environment bodies and a valid
  0.76.0-to-candidate version pair using the actual hermetic producer. Cover equal
  mutations on both sides, digest-only repairs leaving stale receipts, unchanged
  output on invalid input, and direct comparison-helper digest checks.
- Green regressions prove cross-version comparability while third-party/server/BCP
  versions, resources and their types, layout, additional version keys and dpone-key
  presence still reject drift. Preserve same-version, SKIP/UNVERIFIED, canonical
  ordering, invalid-limit errors and baseline run/inspect/help behavior.
- Existing synthetic consumer fixtures use actual canonical configuration and
  environment hashes. Historical retained evidence is never rewritten to pass.
- Update the certification, observations and operations guidance and changelog.
  Run focused tests, types, lint/format, canonical import/layer/size/fitness gates,
  generated metrics freshness/idempotence and documentation checks. Keep all
  thresholds/baselines unchanged and retain actual failures.
- A fresh-context reviewer independently checks the implemented corrections and
  integration preservation. Fix any blocking finding and repeat review.
- Preserve linear history and old PRs/branches. If imported history still conflicts,
  freeze the corrected source H and transfer its exact whole tree onto pinned
  upstream in a successor branch; prove staged/committed tree equality, sole parent
  and H provenance before creating the replacement PR. Preserve H and old history.
- The complete required CI population on final head, Python 3.11 and 3.12, satisfies
  the broad non-live gate. Duplicate unchanged full local execution is N/A unless a
  concrete failure needs reproduction. Retain final provider artifacts externally
  to avoid committing a source revision only to describe its own CI.

No merge, release, retagging, live execution or PyPI verification belongs to this
correction. Public native SWITCH remains rejected. Future upstream movement is
assessed explicitly and never silently substituted for this pinned preservation
input.
