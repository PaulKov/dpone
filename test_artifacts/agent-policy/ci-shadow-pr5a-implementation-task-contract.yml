schema_version: 1
task_id: "DPONE-512-PR5A-IMPLEMENTATION"
title: "CI shadow PR5A immutable candidate manifest"
goal: "Produce a bounded, descriptor-confined immutable manifest for exactly three compatibility wheels."
specification: "docs/feature-design-ci-shadow-pr5a-candidate-manifest.md"
base_commit: "aaf1bd842cd487a13e600f5d5c4b0bcb243e770e"
integrator: "dpone-integrator"

acceptance_criteria:
  - "The CLI accepts one confined three-wheel directory and creates a new canonical v1 manifest."
  - "Wrong cardinality, unsafe inputs, version mismatch and existing output fail closed without replacing bytes."
  - "The manifest has a closed v1 schema, canonical digest and no authority beyond diagnostic inventory production."
public_contract_impact:
  level: compatible
  surfaces: ["tools/ci/build_candidate_manifest.py", "compatibility candidate v1 schema", "CI/CD documentation"]
  migration_required: false
owned_paths:
  - "src/dpone/manifest/compatibility_candidate.py"
  - "tools/ci/build_candidate_manifest.py"
  - "docs/schemas/cicd/compatibility-candidate-v1.schema.json"
  - "docs/cicd/exact-sha-compatibility.md"
  - "docs/cicd/runbooks.md"
  - "docs/ci-cd.md"
  - "mkdocs.yml"
  - "tests/test_compatibility_candidate.py"
  - "tests/test_build_candidate_manifest.py"
  - "test_artifacts/agent-policy/ci-shadow-pr5a-implementation-task-contract.yml"
integrator_owned_paths: []
read_only_paths:
  - "docs/feature-design-ci-shadow-pr5a-candidate-manifest.md"
  - "docs/adr/0048-exact-sha-readiness-evidence.md"
  - "src/dpone/manifest/confined_files.py"
forbidden_paths:
  - "pyproject.toml"
  - "uv.lock"
  - "CHANGELOG.md"
  - ".github/workflows"
shared_file_owner: "dpone-integrator"
dependencies:
  - "PR4A integration commit aaf1bd842cd487a13e600f5d5c4b0bcb243e770e"
  - "Approved PR5A specification"
required_checks:
  focused:
    - "uv run python tools/agent_policy/task_contract.py test_artifacts/agent-policy/ci-shadow-pr5a-implementation-task-contract.yml"
    - "uv run pytest tests/test_compatibility_candidate.py tests/test_build_candidate_manifest.py -q"
  broad:
    - "uv run ruff check src/dpone/manifest/compatibility_candidate.py tools/ci/build_candidate_manifest.py tests/test_compatibility_candidate.py tests/test_build_candidate_manifest.py"
    - "uv run dpone docs check-docs"
    - "uv run mkdocs build --strict"
  live:
    - "N/A: PR5A is local diagnostic production only; authoritative hosted evidence is PR5B."
required_outputs: ["implementation", "tests", "documentation_impact", "completion_report"]
completion_statuses:
  allowed: [PASS, FAIL, SKIP, N/A, UNVERIFIED]
stop_conditions:
  - "Required edit falls outside owned_paths."
  - "Approved specification is missing or contradicted."
  - "Public-contract impact is larger than declared."
  - "A live check requires unapproved credentials or environment."
  - "Another writer owns the same semantic contract."
  - "The change introduces candidate execution, GitHub authority, a workflow, network access, or overwrite semantics."
