# dpone Kafka live verification summary

- Date: 2026-06-02
- Tester: Codex
- Run ID: `20260602T155211Z-cb16f40b`
- Scope: Kafka batch source/sink live matrix, Schema Registry formats, local Postgres/ClickHouse/MSSQL targets, regression gates, packaging smoke.

## Local services

- Kafka and Schema Registry: Redpanda container `dpone-it-redpanda`, `127.0.0.1:19092`, `http://127.0.0.1:18081`.
- Postgres: container `dpone-kafka-matrix-postgres`, `127.0.0.1:55433`, database `dpone_it`.
- ClickHouse: container `dpone-kafka-matrix-clickhouse`, native `127.0.0.1:59002`, HTTP `127.0.0.1:58125`, user `default`.
- MSSQL: container `dpone-kafka-matrix-mssql`, `127.0.0.1:11433`, database `dpone`, SQL Server Agent enabled for CDC tests.

## Main artifact

- Matrix Markdown: `test_artifacts/kafka_live_matrix/2026-06-02-20260602T155211Z-cb16f40b-kafka-live-matrix.md`
- Matrix JSON: `test_artifacts/kafka_live_matrix/2026-06-02-20260602T155211Z-cb16f40b-kafka-live-matrix.json`
- Matrix result: `34 passed`, `0 failed`, `1 skipped`.

## Covered live matrix

- Service preflight: Kafka socket and Schema Registry HTTP.
- Kafka sink/source round trip for all load strategies: `full_refresh`, `incremental_append`, `incremental_merge`, `replace`.
- Kafka message formats: `json`, `json_schema`, `avro`, `protobuf`.
- Kafka source bounded read modes: `offsets`, `max_records`, `time_window`.
- Kafka offset state: dpone offset resume.
- Kafka key policies: `unique_key`, `hash_row`, `null`.
- Kafka envelopes and deletes: dpone envelope auto-unwrap and tombstone delete handling.
- Any source to Kafka payload contracts: Postgres in-memory, MSSQL streaming, ClickHouse file artifact, REST API streaming.
- Kafka source to live targets: Postgres, ClickHouse, MSSQL.
- BigQuery live target: skipped because no GCP project credentials were configured for this local run.

## Regression and package gates

```text
uv run ruff check .
Result: passed, "All checks passed!"
```

```text
uv run ruff format --check .
Result: passed, "717 files already formatted"
```

```text
uv run mypy --config-file mypy.ini
Result: passed, "Success: no issues found in 154 source files"
```

```text
DPONE_KAFKA_BOOTSTRAP_SERVERS=127.0.0.1:19092 \
DPONE_SCHEMA_REGISTRY_URL=http://127.0.0.1:18081 \
uv run pytest -m integration_kafka -q
Result: passed, ".. [100%]"
```

```text
DPONE_IT_MSSQL_HOST=127.0.0.1 \
DPONE_IT_MSSQL_PORT=11433 \
DPONE_IT_MSSQL_DATABASE=dpone \
DPONE_IT_MSSQL_USER=sa \
DPONE_IT_MSSQL_PASSWORD=*** \
DPONE_IT_MSSQL_DRIVER='ODBC Driver 18 for SQL Server' \
DPONE_IT_MSSQL_TRUST_SERVER_CERTIFICATE=yes \
DPONE_IT_MSSQL_BCP_PATH=/opt/homebrew/bin/bcp \
uv run pytest -m integration_mssql -q
Result: passed, "... [100%]"
```

```text
DPONE_KAFKA_BOOTSTRAP_SERVERS=127.0.0.1:19092 \
DPONE_SCHEMA_REGISTRY_URL=http://127.0.0.1:18081 \
uv run pytest -m "not integration_live and not integration_mssql" -q
Result: passed, "[100%]"
```

```text
uv build
Result: passed, built dist/dpone-0.1.0.tar.gz and dist/dpone-0.1.0-py3-none-any.whl
```

```text
uvx twine check dist/*
Result: passed for wheel and sdist
```

```text
Fresh venv install: dist/dpone-0.1.0-py3-none-any.whl[kafka]
Result: passed, imported JSON Schema, Avro and Protobuf Schema Registry SerDes plus Kafka runtime classes
```

## Notes

- `dpone[kafka]` now includes `googleapis-common-protos>=1.70,<2`, required by Confluent Protobuf SerDe fresh installs.
- Kafka Protobuf source/sink live paths support `protobuf_message_class` from runtime options as a generated class or import path.
- BigQuery live remains the only unexecuted live target in this local Kafka matrix because it requires external GCP credentials and project configuration.
