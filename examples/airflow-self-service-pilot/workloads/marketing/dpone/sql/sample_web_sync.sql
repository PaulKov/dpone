-- Placeholder extract for the declarative Airflow pilot layout.
-- Replace with the real marketing query before production use.
SELECT
    cast(1 AS Int64) AS id,
    cast('demo' AS String) AS event_name
FROM system.one
WHERE 0
